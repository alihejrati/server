<script>
    let services = JSON.parse('<%-JSON.stringify(variables.service)%>') || {};
    function service(...arg) {
        const type = arg[0];

        if (type == 'tools') {
            const tool = arg[1]
            const route = services[tool];
            const json = {};
            $.post(route, json, res => {
                console.log('---------->', res);
            });
            console.log('hoooooooooooooo!!', tool, route);
        }
    }
</script>